const FEAT = 'Counter';

describe(`${FEAT}`, () => {
  it(`${FEAT} Snapshot`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onClick Add`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onClick Remove`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onClick OneByOne`, () => {
    expect(false).toBeTruthy();
  });
});
