const FEAT = 'TextFields';

describe(`${FEAT}`, () => {
  it(`${FEAT} Snapshot`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} props name`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} props label`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} props placeholder`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} props value`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onChange`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onBlur`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onFocus`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} input type`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} disabled`, () => {
    expect(false).toBeTruthy();
  });
});
