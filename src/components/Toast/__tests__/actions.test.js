const FEAT = 'Toast Action';

describe(`${FEAT}`, () => {
  it(`${FEAT} setMessage`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} toggleActive`, () => {
    expect(false).toBeTruthy();
  });
});
