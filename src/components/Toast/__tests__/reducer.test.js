const FEAT = 'Toast Reducer';

describe(`${FEAT}`, () => {
  it(`${FEAT} setMessage`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} toggleActive`, () => {
    expect(false).toBeTruthy();
  });
});
