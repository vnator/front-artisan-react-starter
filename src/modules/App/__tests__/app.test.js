const FEAT = 'App';

describe(`${FEAT}`, () => {
  it(`${FEAT} Snapshot`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} menu counter`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} menu address`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} menu users`, () => {
    expect(false).toBeTruthy();
  });
});
