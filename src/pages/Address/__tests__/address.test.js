const FEAT = 'Address';

describe(`${FEAT}`, () => {
  it(`${FEAT} Snapshot`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onClick select address`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onChange street`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onClick update street`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onChange cities`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} onClick cities`, () => {
    expect(false).toBeTruthy();
  });
});
