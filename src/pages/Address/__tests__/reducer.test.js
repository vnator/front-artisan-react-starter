const FEAT = 'Address Reducer';

describe(`${FEAT}`, () => {
  it(`${FEAT} setStreet`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} setNumber`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} setCity`, () => {
    expect(false).toBeTruthy();
  });
});
