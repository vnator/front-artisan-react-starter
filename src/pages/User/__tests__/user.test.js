const FEAT = 'UserList';

describe(`${FEAT}`, () => {
  it(`${FEAT} Snapshot`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onClick orderByName`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onClick orderByEmail`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onClick page Up`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onClick page Down`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onClick edit user`, () => {
    expect(false).toBeTruthy();
  });

  it(`${FEAT} onClick new user`, () => {
    expect(false).toBeTruthy();
  });
});
