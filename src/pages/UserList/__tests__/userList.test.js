const FEAT = 'Toast';

describe(`${FEAT}`, () => {
  it(`${FEAT} Snapshot`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} message`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} active`, () => {
    expect(false).toBeTruthy();
  });
  it(`${FEAT} auto hide`, () => {
    expect(false).toBeTruthy();
  });
});
